import React from "react";
import "./Header.css";

const Header = () => {
  return (
    <div className="header">
      <button className="header__btn"> = </button>
    </div>
  );
};

export default Header;
